import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.environmental.dashboard.assistant',
  appName: 'environmental-dashboard-assistant',
  webDir: 'dist'
};

export default config;
