import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'environmental-dashboard-assistant',
  webDir: 'dist'
};

export default config;
